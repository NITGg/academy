Drop Lottie animation files (.json or .lottie) here for Child Mode.
Then register each path in lib/core/assets/playful_assets.dart (kPlayfulAnimationPaths).
Slots with no file registered simply show nothing (fallback-safe).
