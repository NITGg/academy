define("theme_nit/eitheror",["exports"],(function(_exports){Object.defineProperty(_exports,"__esModule",{value:!0}),_exports.init=void 0;/**
 * Say *why* one of two either/or fields is locked, and offer the way out.
 *
 * Core's forgotten-password form disables the e-mail box the moment the
 * username box has a value, and vice versa (`disabledIf` in
 * login/forgot_password_form.php). Core updates that state on blur, so the
 * click that leaves the username box is the same click that lands on a box
 * that has just stopped accepting input - and nothing on the page says so.
 * On the dark brand the disabled state is barely a shade darker, so the
 * learner reads it as "the site will not let me type" rather than "one or
 * the other".
 *
 * This module does not change the rule - the server still refuses a form
 * with both filled. It watches the `disabled` attribute core toggles and,
 * while a box is locked, prints under it which field is holding it and a
 * button that clears that field and hands focus back. The row also gets a
 * class the stylesheet dims, so the state is visible before it is read.
 *
 * Authored as a native ES module - no jQuery. Built to amd/build.
 *
 * @module     theme_nit/eitheror
 * @copyright  2026 NIT
 * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */const DONE="nit-eitheror-enhanced",LOCKED="nit-eitheror-locked",rowOf=e=>e.closest(".fitem")||e.parentElement,buildHint=(e,o)=>{const n=document.createElement("div");n.className="nit-eitheror-hint form-text",n.setAttribute("role","status"),n.hidden=!0;const s=document.createElement("span");s.className="nit-eitheror-text",s.textContent=e;const t=document.createElement("button");return t.type="button",t.className="btn btn-link p-0 align-baseline nit-eitheror-clear",t.textContent=o,n.append(s," ",t),{hint:n,button:t}},wire=(e,o,n)=>{if(e.classList.contains(DONE))return;e.classList.add(DONE);const s=rowOf(e),{hint:t,button:i}=buildHint(n.locked,n.clear),a=s.querySelector(".form-control-feedback");a?a.after(t):e.after(t);const c=()=>{const r=e.disabled;t.hidden=!r,s.classList.toggle(LOCKED,r)};i.addEventListener("click",()=>{o.value="",o.dispatchEvent(new Event("input",{bubbles:!0})),o.dispatchEvent(new Event("change",{bubbles:!0})),c(),e.disabled||e.focus()}),s.addEventListener("click",r=>{e.disabled&&!t.contains(r.target)&&(t.classList.remove("nit-eitheror-nudge"),t.offsetWidth,t.classList.add("nit-eitheror-nudge"),i.focus())}),new MutationObserver(c).observe(e,{attributes:!0,attributeFilter:["disabled"]}),c()},init=e=>{const o=document.querySelector(e.form);o&&(e.pairs||[]).forEach(n=>{const s=o.querySelector(`[name="${n.name}"]`),t=o.querySelector(`[name="${n.lockedBy}"]`);s&&t&&wire(s,t,n.strings)})};_exports.init=init;}));
